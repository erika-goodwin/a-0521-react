//initial state
const INITIAL_STATE = {
    currentUser: null
}

//reducer
const userReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default userReducer;